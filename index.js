/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import ReactStructureApp from './src/containers/ReactStructureApp';

AppRegistry.registerComponent(appName, () => ReactStructureApp);
